#!/usr/bin/env bash
# mise description="Start a kubernetes cluster"

NODE_COUNT=$1
if [ -z "$NODE_COUNT" ]; then
  NODE_COUNT=1
fi

minikube --profile eval \
  --addons=ingress,metrics-server \
  --cpus="4" \
  --nodes="$NODE_COUNT" \
  start
